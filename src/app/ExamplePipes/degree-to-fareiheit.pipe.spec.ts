import { DegreeToFareiheitPipe } from './degree-to-fareiheit.pipe';

describe('DegreeToFareiheitPipe', () => {
  it('create an instance', () => {
    const pipe = new DegreeToFareiheitPipe();
    expect(pipe).toBeTruthy();
  });
});
